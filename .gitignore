# Secrets / local env
.env
.env.local

# Dependencies
node_modules/
web/node_modules/
web/dist/
api/vendor/

# IDE / editor
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Expo / Metro caches
.expo/
expo-app/.expo/
expo-app/dist/
.metro-health-check*

# Logs
*.log
npm-debug.log*

# Per-project ephemeral assistant scratchpads
expo-app/.claude/

# Old native Android scaffold (replaced by Expo)
# (Already removed in the source tree; kept here so it stays gone.)
android/.gradle/

# Stray Gradle build artifacts at repo root (binary junk — never commit;
# these were what made `git add`/push choke). Ignore at root level.
/.gradle/
/build/
expo-app/.gradle/
expo-app/build/
expo-app/android/.gradle/
expo-app/android/build/
